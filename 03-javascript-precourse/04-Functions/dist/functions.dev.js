"use strict";

//Challenge
// 1. write a function to find the parameter of a rectangle
var calculatePerimeter = function calculatePerimeter(width, height) {
  return width + width + height + height;
};

console.log(calculatePerimeter(1, 1)); // 2. Write a function to find the area of a circle when given a radius, print the area to the console
// 3. Do the same to find the circumference of a circle